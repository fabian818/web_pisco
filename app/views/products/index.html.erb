<h1>Products#index</h1>
<p>Find me in app/views/products/index.html.erb</p>
<ul>
	<% @products.each do |p| %>
		<li>
			<%= link_to p.name, product_path(winery: params[:winery], product: p.name) %>
		</li>
	<% end %>
</ul>